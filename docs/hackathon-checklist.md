# Agora Hackathon Checklist

## Done Locally

- Luma registration is complete.
- Local MVP runs at `http://127.0.0.1:5173/`.
- The app converts multilingual market signals into an English prediction-market brief.
- The app generates a probability, confidence gate, resolution sketch, risk flags, and Arc-ready evidence packet.
- Unit tests, lint, production build, desktop browser check, and mobile browser check have passed.

## Before Public Submission

- Install `uv` and the ARC CLI when network access is stable.
- Verify the app can read Arc testnet metadata from the CLI or RPC docs.
- Create a public GitHub repository under `Jackey65536`.
- Push this project using the `github-jackey` SSH host alias.
- Join the Canteen Discord and Arc builder Discord manually, then post a short intro.
- Record a short founder/demo video.
- Add the final public repo link and live demo link when the submission form opens.

## Safety Boundary

- No real wallet private keys are needed for the current MVP.
- No real trading or funds are used.
- The current Arc packet is an evidence hash scaffold, not a submitted transaction.
